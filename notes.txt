git init
git config --global user.name ""
git config --global user.email ""

git add .
git commit -m "name of the version"
git status
git log
git reset (file name) - it will take out take file from staged changes
git checkout -- (file name) - it will undo the changes for that file
git log --all --graph
git config --global alias.s "status"
rm -rf .git or Remove-Item -Recurse -Force .git -> this removes the git in your project


PS C:\Users\john aldrin villa\onedrive\Desktop\GIT> git remote add origin https://github.com/JAVCode8/git-lesson.git
PS C:\Users\john aldrin villa\onedrive\Desktop\GIT> git config --global credential.username "adlrin"
git remote add (name) url
git remote remove (name)
git push (name) master



git remote add (name but the naming convention is origin) then url of repository(select https)
git remote - show the list the currently link
girt remote -v 
git remote remove origin
git config --global credential.username "" - to identify the we are the editor
git push (repository name) (master)